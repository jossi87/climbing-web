/**
 * Approach / descent polylines on Leaflet and elevation profile charts (Sector, Problem, Area).
 * Replaces named CSS `lime` / `purple` so map and graph use the same hues on dark UI.
 */
export const SLOPE_APPROACH_COLOR = '#84cc16';
export const SLOPE_DESCENT_COLOR = '#a855f7';
